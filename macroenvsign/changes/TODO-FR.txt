===================
macroenvsign - TODO
===================


> ---------------------------- <

gestion auto de s pour version stra ou non pour faire comme suit :

%%%
% prototype::
%     #2 : a one-line descriptive text
%     #3 : xxx
%     #4 : xxx
%     #5 : a path of a file
%
% This macro is similar to ''\inputbdoclatexbefore'' except that the output
% is put inside an environment ''bdocshowcase'' instead of being centered.
%%%
\NewDocumentCommand{\bdoclatexshow}{
  s
  D<>{\bdoc@trans@this@gives \bdoc@colon}
  O{\bdoc@trans@latex@show@start}
  O{\bdoc@trans@latex@show@end}
  m
}{


> ---------------------------- <

    \begin{idmacro}{
        name  = pderope,
        about = {p-artial, der-ivation, ope-rator}
    }
        \arg[in = {f, sf, i}] Voir la fiche technique de \docmacro{pder} juste avant.

        \arg[csv] Les variables utilisées avec leur ordre de dérivation via la syntaxe indiquée ci-dessus.

        \arg L'ordre total de dérivation.
    \end{idmacro}

> ---------------------------- <

gestion des xtemplates aussi !


> ---------------------------- <

Nelle API !

NEW     Top !!!!
---

    signature auto via latex3 : O{f}mm

    in = valeurs, possibles
    svby = ch-aeractères employés pour spéarer des vals

    \begin{idmacro}
        name  = pderope,
        about = {p-artial, der-ivation, ope-rator}
    }
        \arg[in = {f, sf, i}] Voir la fiche technique de \docmacro{pder} juste avant.

        \arg[svby = |] Les variables utilisées avec leur ordre de dérivation via la syntaxe indiquée ci-dessus.

        \arg L'ordre total de dérivation.
    \end{idmacro}



    ???  1. Creation of an environment ''\env{IDsign}'' to manage all cases previously taken into account by the "deceased" macros ''\macro{IDmacro}'', ''\macro{IDenv}'' and ''\macro{IDope}'' (the signatures are displayed inside an invisible table such as to see a frame with a background color).

    ???  1. There is no longer restriction in the way to indicate options and arguments. For example, latex::''h3B2R5'' will print latex::''[#opts]{#1..#2}<#1..#5>'' (the syntax is natural and easy-to-use: see the \doc).

        B(race) argument
        b(race) options

        h(ook)
        r(after)

    ???  1. The new environment ''\env{IDabout}'' allows to add small \infos about a signature (this can be used typically to explain prefixes and suffixes).

    ???  1. The new macro ''\signprewhy'' can be used to show shortly the origin of a prefix or a suffix.


OLD
---

    \IDmacro{pderope}{1}{2}    \signprewhy*{p}{artial}

                               \signprewhy*{ope}{rator}

    \IDoption{} la valeur par défaut est \verb+f+. Les options disponibles sont \verb+f+, \verb+sf+ et \verb+i+ : voir la fiche \tech de \docmacro{pder} juste avant.

    \IDarg{1} les variables utilisées avec leur ordre de dérivation via la syntaxe indiquée ci-dessus.

    \IDarg{2} l'ordre total de dérivation.


> ---------------------------- <

changes

    --> tout mettre au format des chtgs sméantiques


> ---------------------------- <

ficheir résumé nommé via fichiertext.sign.txt
    ---> à ajouter dans gitignore du coup !

> ---------------------------- <

???? espace extra pour IDkey et ajout de IDstyle
